* Configure support for large-language models (LLMs)

#+BEGIN_SRC emacs-lisp

    (use-package gptel
      :ensure t
      :config
      (gptel-make-gpt4all "GPT4All:llama-3.1"
        :stream t
        :protocol "http"
        :host "localhost:4891"
        :models '("Meta-Llama-3.1-8B-Instruct-128k-Q4_0.gguf")
        )
      (gptel-make-gpt4all "GPT4All:codeqwen-1.5"
        :stream t
        :protocol "http"
        :host "localhost:4891"
        :models '("codeqwen-1_5-7b-chat-q4_0.gguf")
        )
      (gptel-make-openai "llamafile:llava-v1.5-7b-q4"
        :stream t
        :protocol "http"
        :host "localhost:8091"
        :models '("test")
        )
      (gptel-make-openai "llamafile:wizardcoder-python-34b-v1.0.Q5_K_M"
        :stream t
        :protocol "http"
        :host "localhost:8092"
        :models '("test")
        )
      (gptel-make-openai "llamafile:Meta-Llama-3-70B-Instruct.Q4_0"
        :stream t
        :protocol "http"
        :host "localhost:8093"
        :models '("test")
        )
      (gptel-make-openai "llamafile:Meta-Llama-3-8B-Instruct.Q5_K_M"
        :stream t
        :protocol "http"
        :host "localhost:8094"
        :models '("test")
        )
      (add-hook 'gptel-post-stream-hook 'gptel-auto-scroll)
      (add-hook 'gptel-post-response-functions 'gptel-end-of-response)
      )

#+END_SRC
